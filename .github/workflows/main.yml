on:
  - push
  - workflow_dispatch

env:
  FOO: bar

jobs:
  main:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        x: [${{ env.FOO }}]
    outputs:
      x: ${{ matrix.x }}
    steps:
      - run: echo ${{ env.FOO }}

  bla:
    runs-on: ubuntu-latest
    needs: main
    steps:
      - run: echo ${{ needs.main.outputs.x }}
