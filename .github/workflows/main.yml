on:
  push: {}
  workflow_dispatch:
    inputs:
      foo:
        description: foo
        required: true
        default: ${{ github.actor }}
        type: ${{ 'string' }}

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
    - run: echo ${{ inputs.foo }}
