on:
  pull_request:
    branches:
      - main
  workflow_dispatch: {}

jobs:
  check-vendor:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Check vendor
        run: |
          bazel run :vendor
          git add .
          git diff HEAD --exit-code
